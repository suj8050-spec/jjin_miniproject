name: 🤖 Tistory Auto Post

on:
  schedule:
    # 매일 오전 9시 (KST) = UTC 00:00
    - cron: '0 0 * * *'
  workflow_dispatch:
    # 수동 실행 버튼 (테스트용)
    inputs:
      reason:
        description: '실행 이유'
        required: false
        default: '수동 테스트'

jobs:
  auto-post:
    runs-on: ubuntu-latest
    
    # posted_urls.json 유지를 위해 권한 필요
    permissions:
      contents: write

    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4

      - name: 🐍 Python 3.11 세팅
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 📦 의존성 설치
        run: pip install -r requirements.txt

      - name: 🚀 자동 포스팅 실행
        env:
          NEWS_API_KEY: ${{ secrets.NEWS_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          TISTORY_ACCESS_TOKEN: ${{ secrets.TISTORY_ACCESS_TOKEN }}
          TISTORY_BLOG_NAME: ${{ secrets.TISTORY_BLOG_NAME }}
        run: python main.py

      - name: 💾 포스팅 기록 커밋 (중복 방지)
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add posted_urls.json || true
          git diff --staged --quiet || git commit -m "📝 포스팅 기록 업데이트 $(date '+%Y-%m-%d')"
          git push || true
